## 2025-12-23
- フレーバー一覧でメーカー単位のグルーピング表示を切り替えられるようにしました。

## 2025-12-19
- フレーバー一覧のタグ一覧に固定タグ「ダークリーフ」を追加し、データがなくてもフィルターで選択できるようにしました。
- ホームのカレンダーで日付をクリックした際のモーダルに、ミックス名・使用フレーバー構成・場所・メモなど詳細情報を表示するようにしました。
- カレンダーモーダルからセッションの編集（日時・満足度・ミックス・場所・メモ）と削除ができるようにし、更新後は一覧を再取得して反映するようにしました。
- カレンダーモーダルからX（Twitter）への投稿ボタンを追加し、ミックス・満足度・フレーバー構成・メモ付きで intent URL を開くようにしました。
- スマホ環境でX投稿ボタンが動作するよう、意図URLを a タグで開く方式に変更し、window.open 依存をなくしました。
- intent URL を `x.com/intent/tweet` に変更し、ユニバーサルリンク経由でアプリが開く可能性を高めました（ブラウザ依存）。
- 環境変数の読込に dotenvx を追加し、`npm run dev/build/start/preview:cf` で `.env` / `.env.local` / `.env.vault` を自動適用するようにしました。
- Docker ベースのローカル開発環境（Dockerfile / docker-compose / .dockerignore）を追加し、セッションフォームやホームのカレンダービューを改善しました。

## 2025-12-16
- Cloudflare Pages 向けのビルドエラーを解消し、デプロイが通るようにしました。

## 2025-12-15
- 認証モーダルのアクセシビリティと UX を改善しました。

## 2025-12-12
- モーダル型のサインイン UI とレスポンシブなナビゲーションを導入し、サインイン状態更新時の不要なリフレッシュを防止しました。
- セッション周りの UI を改善し、全体のビルド安定性を向上させました。

## 2025-11-12
- ミックス作成のスライダーを同期させ、フレーバー数を柔軟に扱えるようにしました。
- テーマ切り替え UI を再設計し、ダークモードとラベル表示を追加しました。
- セッションのログページを追加し、サンプルフレーバーのシードデータを拡充しました。
- ブランド/フレーバーの新規作成フローを追加し、トップページを Edge Runtime で動かしつつホームリンクやシード用マイグレーションを整備しました。
- Google 認証リダイレクトや Edge ルート周りの不具合を修正しました。

## 2025-11-11
- ミックスのダッシュボードを実装し、編集・削除をサポートしました。

## 2025-11-10
- Email / Google サインインを追加し、`/mixes/new` を Edge Runtime 対応にしました。
- フレーバー一覧を検索可能にし、ホームからの導線を追加しました。
- Cloudflare Pages でのビルドを安定化し、ブランド・フレーバーの型整合性を修正しました。
- プロジェクト共通設定ファイルを追加し、Node.js 互換設定を有効化しました。

## 2025-11-06
- Next.js + Supabase 構成で Kemureco アプリをブートストラップしました。

## 2025-11-05
- 初回コミット。
